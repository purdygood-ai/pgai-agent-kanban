[workflow]
name = release
description = Software release with git RC branch lifecycle (open-rc -> implement -> verify -> release). Semver versioning, read-write git, finalize by tag.
status = ready

[capabilities]
version_semantics = semver
git_mode = rw
finalize = tag
agents = pm,coder,writer,tester,cm
