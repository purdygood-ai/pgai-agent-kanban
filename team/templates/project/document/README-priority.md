# Priority Items

Operator-authored out-of-cycle requests (not bugs). Bundled by the discovery
pipeline (Step 2).

Naming: `PRIORITY-NNNN-<slug>.md`. `## Status`: `open` | `running` | `done`.
Cache: `tasks/queues/priority_backlog.md`.
