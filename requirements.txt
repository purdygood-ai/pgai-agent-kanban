# pgai-agent-kanban — runtime dependencies
#
# Python packages the running kanban imports. The framework is otherwise
# standard-library only.
#
# Install (into the venv the agents run under):
#   pip install -r requirements.txt --break-system-packages
#
# Note: the kanban targets Python >= 3.12 (see team/pyproject.toml).

# Workflow YAML parsing (team/pm-agent/lib/workflow_loader.py).
PyYAML>=6.0

# Operator REST API (team/pgai_agent_kanban/api/). [standard] extra pulls
# httpx, required by fastapi.testclient.TestClient in the API test suites.
fastapi[standard]>=0.100

# Data validation; used directly via BaseModel in
# team/pgai_agent_kanban/api/routers/operations.py and
# team/scripts/lint_api_parity.py.
pydantic>=2.0

# ASGI server that runs the operator REST API; imported directly in
# team/pgai_agent_kanban/api/main.py. [standard] extra adds websocket support.
uvicorn[standard]>=0.20
